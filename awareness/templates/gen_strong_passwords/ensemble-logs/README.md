# gen_strong_passwords / ensemble-logs

Per-template AI prompt+response logs for the **gen_strong_passwords** template
(status: testing), mirrored from the canonical project-root `ensemble-logs/`.

Expected log here:
- `strong_passwords_advisory.txt` — the AI advisory-line call (`nlStrongPwAdvisory`)
  that fills the footer/closing advisory (SECTION3_BULLET1).

Folder kept under version control via this README (zip strips empty folders).
