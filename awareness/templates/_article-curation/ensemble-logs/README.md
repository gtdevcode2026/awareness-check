# _article-curation / ensemble-logs

AI prompt + response logs for the **article curation** step (per-article summarization that runs before any template is selected).

These logs aren't tied to any one template — they're the upstream AI work that feeds into every template build.

Each curation pass creates a fresh timestamped subfolder.

Folder kept under version control via this README (zip strips empty folders).

